// ReSharper disable InconsistentNaming
// lowercase for consistent casing with command line options.
namespace DbUp.Cli.Cmd;

public enum VerbosityLevel
{
    // now that this is case-sensitive, 
    detail,
    normal,
    min
}